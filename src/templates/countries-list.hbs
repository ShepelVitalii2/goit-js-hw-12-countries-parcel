<ul class="countries-list">
    {{#each this}}
    <li class="countries-list__item">
        <img class="coutries-list__img" src="{{flag}}" width="100" height="auto" alt="flag">
        <span class="country-name">{{name}}</span>
    </li>
    {{/each}}
</ul>